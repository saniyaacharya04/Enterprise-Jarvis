version: "3.9"

services:
  jarvis-backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env

  jarvis-frontend:
    image: streamlit/streamlit
    volumes:
      - ./frontend:/app
    command: streamlit run streamlit_app.py
    ports:
      - "8501:8501"
